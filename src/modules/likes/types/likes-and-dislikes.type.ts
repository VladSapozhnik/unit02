export class LikesAndDislikesType {
  likesCount: number;
  dislikesCount: number;

  constructor(likesCount: number, dislikesCount: number) {
    this.likesCount = likesCount;
    this.dislikesCount = dislikesCount;
  }
}
